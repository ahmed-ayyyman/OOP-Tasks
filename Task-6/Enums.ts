export enum RoomType {
  Single,
  Double,
  Suite,
  Deluxe,
  Presidential,
}

export enum RoomStatus {
  Available,
  Occupied,
  UnderMaintenance,
  Reserved,
}

export enum ReservationStatus {
  Pending,
  Confirmed,
  CheckedIn,
  CheckedOut,
  Cancelled,
}
